#include <stdio.h>
#include <string.h>
#include <math.h>
#include <stdlib.h>
int main(){
    int N, search, found = 0;
    scanf("%d", &N);
    int arr[N];
    for (int i = 0; i < N; i++){
        scanf("%d", &arr[i]);
    }
    scanf("%d", &search);
    for (int i = 0; i < N; i++){
        if (arr[i] == search){
            found = 1;
            break;
        }
    }
    if (found){
        printf("Found");
    } else{
        printf("Not Found");
    }
     return 0;
}
